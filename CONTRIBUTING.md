# 開発の進め方（CONTRIBUTING）

このリポジトリは「本人がレビュアー / Claude が実装担当」体制で進める練習プロジェクト。
小さく作って、毎回レビューし、説明できる状態でマージする。

## ブランチ運用
- `main` は常に動く（CI が緑）状態を保つ。直接 push しない。
- 機能追加・修正は `feat/...` `fix/...` のブランチを切って作業する。

## PR の流れ
1. ブランチを切る: `git checkout -b feat/xxx`
2. 実装 + テストを書く
3. `uv run pytest` が通ることを確認
4. push して PR を作成（テンプレートに沿って「何を・なぜ」を書く）
5. **セルフレビュー**: 差分を自分で読み、設計判断を説明できるか確認
6. CI が緑になったらマージ

## コミットメッセージ
- 日本語可。「何を・なぜ」が分かる粒度で。

## テスト
- 正常系だけでなく異常系（失敗時に正しく失敗するか）も書く。
- `uv run pytest` でカバレッジ付きで実行される。
